name: CI

on:
  push:
<<<<<<< HEAD
    branches: [main, develop]
=======
    branches: [main]
>>>>>>> fe6b5205cd17925779c19f6fcc20846e121532bf
  pull_request:
    branches: [main]

jobs:
  backend:
<<<<<<< HEAD
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'
      - run: pip install -r backend/requirements.txt pytest coverage ruff
      - run: ruff check backend/
      - run: coverage run -m pytest backend/tests && coverage xml
      - uses: codecov/codecov-action@v4
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: coverage.xml
          flags: backend

  frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json
      - run: npm ci --prefix frontend
      - run: npm run lint --prefix frontend
      - run: npm test --prefix frontend
      - uses: codecov/codecov-action@v4
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: frontend/coverage/lcov.info
          flags: frontend
=======
    name: Backend (Java / Maven)
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven

      - name: Run tests & generate JaCoCo report
        working-directory: backend
        run: mvn -B test
        env:
          # 测试全部使用 Mockito（@WebMvcTest + @ExtendWith(MockitoExtension.class)），
          # 不启动完整 Spring 上下文，无需真实数据库
          AI_API_KEY: dummy-key-for-ci

      - name: Print coverage summary
        working-directory: backend
        run: mvn -B jacoco:report && cat target/site/jacoco/index.html | grep -o 'Total[^%]*%' | head -5 || echo "Coverage report generated at target/site/jacoco/index.html"

  # frontend:
  #   name: Frontend (React / TypeScript)
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - name: Set up Node.js 20
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: '20'
  #         cache: npm
  #         cache-dependency-path: fronted/package-lock.json
  #     - name: Install dependencies
  #       working-directory: fronted
  #       run: npm ci
  #     - name: Type check
  #       working-directory: fronted
  #       run: npx tsc --noEmit
  #     - name: Build
  #       working-directory: fronted
  #       run: npm run build
>>>>>>> fe6b5205cd17925779c19f6fcc20846e121532bf
